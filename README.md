# Ordis-GesAc

Sistema de Gestión Académica.

## Descripción
Proyecto para la gestión integral de instituciones académicas.

## Estructura del proyecto
- `src/`: Código fuente del proyecto Laravel.
- `docker/`: Configuración de Docker.

## Instalación

1. Clonar el repositorio.
2. Copiar `.env.example` a `.env` en la carpeta `src/`.
3. Ejecutar `docker-compose up -d`.
4. El proyecto estará disponible en `http://localhost:8000`.

## Contribución
(Instrucciones de contribución por definir)
